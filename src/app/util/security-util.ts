//@Injectable
export class SecurityUtil {

  constructor( // TODO encryption
    //private hashEncoder:
  ) {
  }
  static getPasswordHash(rawPassword: string): string {
    return rawPassword;
  }
}
